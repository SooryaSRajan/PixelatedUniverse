<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssrprojects.cosmopent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pixelated Universe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="false"
        android:theme="@style/AppTheme">
        <activity android:name=".TelescopeDataActivity"></activity>
        <activity android:name=".TestData" />
        <activity android:name=".HomePageFragments.ContourMappingActivity" />
        <activity android:name=".SpaceTelescopeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".LoginSignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>